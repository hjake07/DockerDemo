{
  "name": "dockerfile",
  "version": "1.0.0",
  "description": "Hello This is a Docker Demo Where We Are Trying To Create a Container",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "jake",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "os": "^0.1.2"
  }
}
